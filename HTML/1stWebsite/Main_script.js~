 var urlMenu = document.getElementById('Language');
	urlMenu.onchange = function () 
	{
		var userOption = this.options[this.selectedIndex];
	
		if (userOption.value != "nothing") 
		{
		}
			window.open(userOption.value, "LNG page", "");
	}